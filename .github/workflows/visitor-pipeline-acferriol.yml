name: Visitor Counter API - acferriol

on: 
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

jobs: 
    linting:
        runs-on: ubuntu-latest
        name: "Linting"
        steps:
            - uses: actions/checkout@v4
            - uses: ludeeus/action-shellcheck@master
              with:
                severity: 'error'
    
    
    security:
        needs: linting
        runs-on: ubuntu-latest
        name: "Security"
        steps:
            - uses: actions/checkout@v4
            - uses: aquasecurity/trivy-action@master
              with:
                severity: 'CRITICAL'
                scan-type: fs
                scan-ref: .
    
    build-publish:
        needs: [linting, security]
        runs-on: ubuntu-latest
        name: 'Build and Publish'
        permissions:
            contents: read
            packages: write 
        steps:
            - uses: actions/checkout@v4
            - uses: docker/login-action@v3
              with: 
                registry: ghcr.io
                username: ${{ github.actor }}
                password: ${{ secrets.GITHUB_TOKEN }}
            - uses: docker/build-push-action@v5
              with:
                context: .
                file: ./docker-acferriol/Dockerfile
                push: true
                tags: |
                    ghcr.io/${{ github.actor }}/visitors:${{ github.sha }}
                    ghcr.io/${{ github.actor }}/visitors:latest




